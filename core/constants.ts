/** Minimum desktop grid cell size (px). */
export const MIN_GRID_SIZE = 100;
/** Maximum desktop grid cell size (px). */
export const MAX_GRID_SIZE = 120;
/** Default desktop grid cell size (px). */
export const DEFAULT_GRID_SIZE = 100;

/** Minimum desktop icon glyph size (px). */
export const MIN_ICON_SIZE = 32;
/** Default desktop icon glyph size (px). */
export const DEFAULT_ICON_SIZE = 64;
/** Small desktop icon preset (px). */
export const ICON_SIZE_SMALL = 48;
/** Medium desktop icon preset (px). */
export const ICON_SIZE_MEDIUM = DEFAULT_ICON_SIZE;
/** Large desktop icon preset (px). */
export const ICON_SIZE_LARGE = 80;

/** Horizontal + vertical padding inside a grid cell (px). */
export const ICON_CELL_PADDING = 8;
/** Space reserved below the icon for its label (px). */
export const ICON_LABEL_SPACE = 22;

/** SVG glyph scale relative to icon size. */
export const ICON_GLYPH_SCALE = 0.8;
/** Emoji glyph scale relative to icon size. */
export const ICON_EMOJI_SCALE = 0.7;

/** Collision / row grouping uses half a cell. */
export const GRID_OCCUPANCY_RATIO = 0.5;

/** Dock / usable viewport taskbar height (px). */
export const TASKBAR_HEIGHT = 48;

/** Pixels of movement before a desktop icon drag starts. */
export const DRAG_START_THRESHOLD = 5;
